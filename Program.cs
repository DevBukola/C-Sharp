using System;
using System.Collections.Generic;
using System.Globalization;
using System.IO.Pipelines;
using System.Linq;
using System.Security.Cryptography;
using System.Text;
using System.Threading.Tasks;

namespace HelloWorld
{
    class Program {
        static void Main(string[] args)
        {
           
        }
    }
}

/*
Console is a built-in C# class that lets the program communicate through the terminal. It is my program way of talking to me - It can be thought of as that.
.(Dot): The dot means "use something that belongs to the console". i.e, use the WriteLine functionality from the cosole.
WriteLine is a method that performs an action. It prints something, then move to the next line.
Semicolon(;) tells C#: "This instruction is finished." It is similar to a period(.) after a sentence in English. Without it, the compiler usually won't where one statement ends and the next begins.
*/